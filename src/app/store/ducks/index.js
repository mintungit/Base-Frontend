import * as app from '@store/ducks/app.duck';
import * as locale from '@store/ducks/locale.duck';
import * as module from '@store/ducks/module.duck';


export const DUCKS = {
  app,
  locale,
  module,
};
  